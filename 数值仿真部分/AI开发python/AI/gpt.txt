我正在制作一款游戏，需要写电脑阵营的ai部分，我用python。
游戏规则如下，地图上有6个节点(或称之为塔)，节点隶属于不同的阵营，例如中立，电脑或玩家。
节点每1.5秒可以产出1个士兵，但中立阵营的节点不产生士兵。
节点中可以屯兵，士兵数量即为节点血量。
节点之间可以连接，例如Node A连接Node B后，如果A和B不同阵营，A就会让所有新增的士兵攻击B。如果A和B相同阵营，那么A会把产生的士兵送给B。
如果节点A没有任何输出士兵的连接，那么A会把自身产出的士兵，或者其他己方节点输入的士兵屯进节点内，增加节点血量。
如果节点A有输出士兵的连接，例如A连接B，那么A就会把把自身产出的士兵，或者其他己方节点输入的士兵，输出给B。
如果节点A连接了B，A和B不属于同一阵营，那么A输出的每一个士兵，B都会减少1点血量，如果B的血量变为-1，那么B会转变为A的阵营。

我需要补充一下游戏规则，节点之间的连接实际是有方向的。例如节点A和B不是同阵营，那么A连接B，代表A进攻B; B连接A，代表B进攻A，两个连接可以同时存在。如果节点A和B是同一阵营，那么只能A连接B，或者B连接A

请你帮我一步一步在python上完成这个ai。

那么首先，我的ai平台应该包含几大部分，帮我用文字说明一下
--------------
能不能帮我搭一个游戏状态管理模块
-------------
我希望塔和连接状态能够在UI上实时显示是否可以
-----------------
我需要补充一下游戏规则，你的连接似乎是无方向的，但是实际是有方向的。例如节点A和B不是同阵营，那么A连接B，代表A进攻B; B连接A，代表B进攻A，两个连接可以同时存在。如果节点A和B是同一阵营，那么只能A连接B，或者B连接A
-----------------
在 class Node: 下面添加一个方法，用于删除一个connection，你只需要显示需要更改的代码
-----------------

我希望你设计一种数据结构X，能够保存当前时刻游戏的状态，包括节点血量，连接状态等，以便我能进行深度搜索。
同时数据结构能够实现剪枝，例如有4个节点A、B、C、D，如果A先连接B，C再连接D，形成状态X1；如果C先连接D，A再连接B，形成状态
X2，那么X1和X2实际是等效的，我希望你的数据结构能够轻易的判断出来这一点。


我希望你重构整个代码，按照我的框架编写几个类和功能：

编辑

类1：GameState类
功能：




